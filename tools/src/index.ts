/**
 * Sports Experience Guidelines — CLI Tooling
 *
 * Entry point for the tools package. Individual CLI commands are:
 * - validate.ts  — Document validation
 * - manifest.ts  — Manifest generation
 * - scaffold.ts  — Document scaffolding
 */

export const VERSION = '0.1.0';
